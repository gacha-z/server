<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dk.gatchaz.server.member.mapper.MemberMapper">

    <insert id="insertMember" useGeneratedKeys="true" keyProperty="memberId" keyColumn="member_id">
        INSERT INTO
            member(
                nickname,
                age,
                deleted_yn,
                created_at
            )
        VALUES
            (
                #{nickname},
                #{age},
                0,
                NOW()
            )
    </insert>

    <select id="selectMember" resultType="dk.gatchaz.server.member.dto.MemberDetailResponse">
        SELECT
            member_id,
            nickname,
            age,
            profile_image_url,
            role,
            created_at,
            updated_at
        FROM member
        WHERE member_id = #{memberId}
          AND (deleted_yn IS NULL OR deleted_yn = 0)
    </select>

    <update id="updateMember">
        UPDATE
            member
        <set>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            updated_at = NOW()
        </set>
        WHERE
            member_id = #{memberId}
            AND (deleted_yn IS NULL OR deleted_yn = 0)
    </update>

    <update id="softDeleteMember">
        UPDATE
            member
        SET
            deleted_yn = 1,
            deleted_at = NOW()
        WHERE
            member_id = #{memberId}
            AND (deleted_yn IS NULL OR deleted_yn = 0)
    </update>

</mapper>
